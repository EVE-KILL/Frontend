export async function load({ params }) {
    return {
        meta: {
            // ShipName | VictimName | SystemName | RegionName | EVE-KILL
            title: `STOMP API`,
            description: `STOMP usage documentation`,
        }
    }
}
